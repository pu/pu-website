%h1 Create new Post

- form_for @post, :url => admin_posts_path, :html => { :class => 'form', :multipart => true } do |form|
  
  = render :partial => 'form', :locals => {:form => form}
  
  = render :partial => 'shared/pictures_upload', :locals => { :f => form }
  
  %p.button
    = form.submit "Create"
    or
    = link_to "Back", admin_posts_path
